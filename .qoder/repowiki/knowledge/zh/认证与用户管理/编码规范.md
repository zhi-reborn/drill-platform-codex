- 服务层方法统一返回业务特定的error变量（如`ErrUserNotFound`），Handler层通过`errors.Is`或字符串匹配转换为对应的HTTP状态码。
- 所有涉及用户敏感信息（如密码哈希）的实体字段在JSON序列化时通过`json:"-"`标签进行隐藏。
- 外部认证配置（CAS/LDAP）采用结构体封装，并提供`Set...Config`方法在服务初始化后动态注入依赖。
- Handler层严格遵循“参数绑定->业务调用->统一响应”的处理范式，不直接在Handler中编写业务逻辑。