- 统一使用 response 包（如 response.Success, response.BadRequest）封装 API 响应格式。
- 服务层方法接收 DTO 或实体指针作为参数，并返回实体指针或错误，保持接口一致性。
- 仓库层使用 GORM Transaction 保证复合操作（如更新步骤、克隆模板）的数据原子性。
- 实体结构体通过 TableName() 方法显式指定数据库表名，遵循 GORM 最佳实践。